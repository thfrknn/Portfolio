# Cloudflare Pages + Functions yapılandırması
# D1 database binding ve environment variables → Cloudflare Dashboard'dan ayarla
# (Aşağıdaki adımları takip et)

name = "portfolio"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."
